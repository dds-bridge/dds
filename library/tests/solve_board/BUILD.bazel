# Solve_board regression tests

load("@rules_cc//cc:defs.bzl", "cc_test")
load("//:CPPVARIABLES.bzl", "DDS_CPPOPTS")


cc_test(
    name = "solve_board_test",
    size = "small",
    srcs = [
        "trick_three_bug.cpp",
    ],
    deps = [
        "//library/src:testable_dds",
        "@googletest//:gtest_main",
    ],
    copts = DDS_CPPOPTS,
)

cc_test(
    name = "analyse_play_consistency_test",
    size = "small",
    srcs = [
        "analyse_play_consistency.cpp",
    ],
    deps = [
        "//library/src:testable_dds",
        "@googletest//:gtest_main",
    ],
    copts = DDS_CPPOPTS,
)

# Quantifies the warm-transposition-table win from reusing one DDS_C_SOLVER_CTX
# across successive similar solves. Binds to the public C ABI (//library/src/api:dds_c_api)
# rather than testable_dds, since it exercises only the exported surface.
cc_test(
    name = "context_warm_tt_benchmark",
    size = "medium",
    srcs = [
        "context_warm_tt_benchmark.cpp",
    ],
    deps = [
        "//library/src:dds",
        "//library/src/api:dds_c_api",
        "@googletest//:gtest_main",
    ],
    copts = DDS_CPPOPTS,
)
