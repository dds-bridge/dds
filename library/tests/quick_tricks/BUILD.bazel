load("@rules_cc//cc:defs.bzl", "cc_test")
load("//:CPPVARIABLES.bzl", "DDS_CPPOPTS")

cc_test(
    name = "next_suit_test",
    size = "small",
    srcs = ["next_suit_test.cpp"],
    deps = [
        "//library/src:testable_dds",
        "@googletest//:gtest_main",
    ],
    copts = DDS_CPPOPTS,
)
